import { AutoNextDirective } from './auto-next.directive';

describe('AutoNextDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoNextDirective();
    expect(directive).toBeTruthy();
  });
});
